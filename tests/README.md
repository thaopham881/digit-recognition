# Tests

This directory will contain automated tests for the core logic of the handwritten digit recognition project.

The planned tests will cover image representation, coordinate-offset generation, nearest-point searches, point-set distance calculations, and k-nearest neighbors classification.

The tests will be developed alongside the implementation, and test coverage will be measured during the project.
